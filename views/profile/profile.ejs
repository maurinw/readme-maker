<h1>Your Saved Readmes</h1>
<div class="profile">
  <% if (readmes.length === 0) { %>
  <p>You haven't saved any readmes yet.</p>
  <% } else { %>
  <ul class="readme-grid">
    <% readmes.forEach(function(readme) { %>
    <li>
      <div>
        <strong><%= readme.title || 'Untitled' %></strong>
      </div>

      <div>
        <!-- Download -->
        <form action="/readme/download" method="get">
          <input type="hidden" name="readmeId" value="<%= readme._id %>" />
          <button type="submit" class="button">Download</button>
        </form>

        <!-- Edit -->
        <form action="/readme/edit" method="get">
          <input type="hidden" name="readmeId" value="<%= readme._id %>" />
          <button type="submit" class="button">Edit</button>
        </form>

        <!-- Delete -->
        <form action="/readme/delete" method="post">
          <input type="hidden" name="readmeId" value="<%= readme._id %>" />
          <button type="submit" class="button button-danger">Delete</button>
        </form>
      </div>
    </li>
    <% }); %>
  </ul>
  <% } %>
</div>
